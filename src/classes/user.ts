export class User {
    id: number
    username: String
    name: String
    active: Boolean

  constructor(id, username, name, active) {
        this.id = id;
        this.username = id;
        this.name =name;
        this.active = active;
    }
}
