import { SettingsMemberFilterPipe } from './settings-member-filter.pipe';

describe('SettingsMemberFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SettingsMemberFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
